name: Update DiziPal Link

on:
  workflow_dispatch:

jobs:
  update-link:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '14'

    - name: Install Puppeteer
      run: npm install puppeteer

    - name: Run JavaScript to update link
      run: node test.js